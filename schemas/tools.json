[
  {
    "agent_instructions": "Call only when the document title or file path is explicitly needed. Do not call this before every action.",
    "parameters": {
      "required": [],
      "type": "object",
      "properties": {}
    },
    "name": "fetch_project_info",
    "description": "Fetches basic identification metadata about the active Revit project. Returns the document title and file path."
  },
  {
    "agent_instructions": "Call before any tool that requires a level_id. Use the returned 'id' (UniqueId) as-is for the level_id argument. The curve_start_x and curve_end_x values indicate the horizontal span of the building \u2014 use these when placing grids or other datum elements that should align with the building.",
    "parameters": {
      "required": [],
      "type": "object",
      "properties": {
        "target_view_id": {
          "description": "Optional UniqueId of a specific section/elevation view to query view-specific (2D) extents, offsets, and bubble visibilities.",
          "type": "string"
        }
      }
    },
    "name": "fetch_levels",
    "description": "Fetches all levels in the active Revit project. Returns each level's name, UniqueId, elevation in feet relative to the Project Base Point, 3D curve extents (curve_start_x, curve_end_x in Revit's internal coordinates), associated Scope Box, structural metadata, level type, and view specific settings."
  },
  {
    "agent_instructions": "Call before create_grid or modify_grid to check existing configurations.",
    "parameters": {
      "required": [],
      "type": "object",
      "properties": {
        "target_view_id": {
          "description": "Optional UniqueId of a specific view to query view-specific (2D) extents, offsets, and bubble visibilities.",
          "type": "string"
        }
      }
    },
    "name": "fetch_grids",
    "description": "Fetches all existing reference gridlines in the active Revit project. Returns each grid's name, UniqueId, start/end coordinates in Revit's internal coordinate system (feet), geometric definition (linear or arc), scope box, type, and view specific settings."
  },
  {
    "agent_instructions": "Call before place_family. Use the exact family_name and type_name strings as returned \u2014 do not approximate or guess names. If the required family is not in the list, inform the user it is not loaded in the project. IMPORTANT: Always specify a category_filter (e.g. 'Doors', 'Windows', 'Furniture') to avoid slow responses from large projects.",
    "parameters": {
      "required": [],
      "type": "object",
      "properties": {
        "category_filter": {
          "description": "Optional category name to filter results (e.g. 'Doors', 'Windows', 'Furniture', 'Plumbing Fixtures'). Leave empty to fetch all categories (may be slow in large projects).",
          "type": "string"
        }
      }
    },
    "name": "fetch_families",
    "description": "Fetches loaded family symbols (types) in the active Revit project. Returns a dictionary mapping family names to lists of their available type names. Optionally filters by category to reduce response size."
  },
  {
    "agent_instructions": "Call before create_sheet. Confirm no sheet with the same sheet_number already exists. Observe the existing numbering convention from the results.",
    "parameters": {
      "required": [],
      "type": "object",
      "properties": {}
    },
    "name": "fetch_sheets",
    "description": "Fetches all existing drawing sheets in the active Revit project. Returns each sheet's number, name, and UniqueId."
  },
  {
    "agent_instructions": "Before calling: (1) Call fetch_levels to get a valid level_id. (2) Call fetch_families to get the exact family_name and type_name. Coordinates x and y are relative to the Project Base Point in feet. Use z=0.0 for level-hosted families unless the user explicitly specifies otherwise.\n(3) NEVER assume the family type, location, or level \u2014 if the user has not specified them, ASK the user to provide these details before calling this tool.",
    "parameters": {
      "required": [
        "family_name",
        "type_name",
        "level_id",
        "x",
        "y",
        "z"
      ],
      "type": "object",
      "properties": {
        "level_id": {
          "description": "The UniqueId of the host level element (from fetch_levels).",
          "type": "string"
        },
        "x": {
          "description": "X coordinate in feet, relative to the Project Base Point.",
          "type": "number"
        },
        "z": {
          "description": "Z coordinate in feet, relative to the Project Base Point. Use 0.0 for level-hosted families.",
          "type": "number"
        },
        "family_name": {
          "description": "The exact name of the family (e.g. 'Single-Flush').",
          "type": "string"
        },
        "type_name": {
          "description": "The exact type name within the family (e.g. '36\" x 84\"').",
          "type": "string"
        },
        "y": {
          "description": "Y coordinate in feet, relative to the Project Base Point.",
          "type": "number"
        }
      }
    },
    "name": "place_family",
    "description": "Places a loaded family symbol instance at a specified coordinate on a given level in the active Revit project."
  },
  {
    "agent_instructions": "PLACEMENT CONTEXT \u2014 read this carefully before choosing coordinates:\n1. ALWAYS call fetch_levels first to get curve_start_x and curve_end_x \u2014 these define the building's horizontal span. Grids should span AT LEAST from curve_start_x to curve_end_x (or wider) so they appear across all levels.\n2. ALWAYS call fetch_grids to check existing grids. If grids already exist, place new ones ADJACENT to them (continue the spacing pattern). Do NOT overlap or place arbitrarily.\n3. If the user specifies spacing (e.g. '10 meters apart'), convert to feet (1m = 3.28084ft) and apply consistently.\n4. If you lack enough information (e.g., no levels exist, user hasn't specified building dimensions), ASK the user for clarification before placing. Do not guess.\n5. GRID ORIENTATION \u2014 critical for crossing grids:\n   - Vertical grids: constant X position, spanning Y from curve_start_x to curve_end_x. Example: start_x=X, start_y=curve_start_x, end_x=X, end_y=curve_end_x.\n   - Horizontal grids: constant Y position, spanning X from curve_start_x to curve_end_x. Example: start_x=curve_start_x, start_y=Y, end_x=curve_end_x, end_y=Y.\n   - ALL grids MUST intersect each other to form a proper grid network. Vertical and horizontal grids must share the same coordinate ranges.\nCOORDINATES: All in Revit's internal coordinate system (feet). Specify either linear (start_x, start_y, end_x, end_y) OR curved (start_x, start_y, end_x, end_y, arc_point_x, arc_point_y OR center_x, center_y, radius, start_angle, end_angle).",
    "parameters": {
      "required": [
        "name"
      ],
      "type": "object",
      "properties": {
        "target_view_id": {
          "description": "The UniqueId of the view where view-specific (2D) adjustments are applied.",
          "type": "string"
        },
        "start_offset": {
          "description": "The coordinate offset distance (feet) to extend or retract the start point of the grid line in a target view.",
          "type": "number"
        },
        "start_y": {
          "description": "Starting Y coordinate in feet, in Revit's internal coordinate system.",
          "type": "number"
        },
        "start_z": {
          "description": "Starting Z coordinate in feet, in Revit's internal coordinate system. Defaults to 0.",
          "type": "number"
        },
        "name": {
          "description": "Unique display name of the grid (e.g. 'A', '1').",
          "type": "string"
        },
        "start_x": {
          "description": "Starting X coordinate in feet, in Revit's internal coordinate system.",
          "type": "number"
        },
        "arc_point_y": {
          "description": "Y coordinate of a third point along the arc in Revit's internal coordinate system (feet).",
          "type": "number"
        },
        "start_angle": {
          "description": "Starting angle of the arc (radians).",
          "type": "number"
        },
        "end_offset": {
          "description": "The coordinate offset distance (feet) to extend or retract the end point of the grid line in a target view.",
          "type": "number"
        },
        "end_y": {
          "description": "Ending Y coordinate in feet, in Revit's internal coordinate system.",
          "type": "number"
        },
        "grid_type_id": {
          "description": "The UniqueId of the Grid Type family symbol to control line patterns and bubble styles.",
          "type": "string"
        },
        "show_bubble_at_end": {
          "description": "Shows the grid bubble at the ending coordinate (defaults to True).",
          "type": "boolean"
        },
        "end_x": {
          "description": "Ending X coordinate in feet, in Revit's internal coordinate system.",
          "type": "number"
        },
        "maximize_3d_extents": {
          "description": "Forces the grid's 3D plane to automatically expand to encompass all model geometry.",
          "type": "boolean"
        },
        "arc_point_z": {
          "description": "Z coordinate of a third point along the arc in Revit's internal coordinate system (feet).",
          "type": "number"
        },
        "arc_point_x": {
          "description": "X coordinate of a third point along the arc in Revit's internal coordinate system (feet).",
          "type": "number"
        },
        "end_angle": {
          "description": "Ending angle of the arc (radians).",
          "type": "number"
        },
        "show_bubble_at_start": {
          "description": "Shows the grid bubble at the starting coordinate (defaults to False).",
          "type": "boolean"
        },
        "center_x": {
          "description": "Center point X coordinate for radial arcs in Revit's internal coordinate system (feet).",
          "type": "number"
        },
        "center_y": {
          "description": "Center point Y coordinate for radial arcs in Revit's internal coordinate system (feet).",
          "type": "number"
        },
        "radius": {
          "description": "Radius distance of the curved arc (feet).",
          "type": "number"
        },
        "scope_box_id": {
          "description": "The UniqueId of an existing Scope Box to control the grid's 3D boundary.",
          "type": "string"
        },
        "propagate_to_views": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "A list of target view UniqueIds that should copy the same 2D grid line endpoints."
        },
        "end_z": {
          "description": "Ending Z coordinate in feet, in Revit's internal coordinate system. Defaults to 0.",
          "type": "number"
        },
        "datum_extent_type": {
          "description": "Options are '2D' (view-specific) or '3D' (model-wide) when making graphic adjustments.",
          "type": "string"
        }
      }
    },
    "name": "create_grid",
    "description": "Creates a new reference gridline in the active Revit project. Supports linear geometry, curved (arc) geometry, physical extents, bubble visibility, and view specific offsets."
  },
  {
    "agent_instructions": "Before calling: (1) Call fetch_grids to get the valid UniqueId for the grid_id parameter. If geometry coordinates are updated, the grid will be adjusted in-place if coincident, or recreated/replaced to support new coordinate layouts.",
    "parameters": {
      "required": [
        "grid_id"
      ],
      "type": "object",
      "properties": {
        "start_y": {
          "description": "Starting Y coordinate in feet, in Revit's internal coordinate system.",
          "type": "number"
        },
        "propagate_to_views": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "A list of target view UniqueIds that should copy the same 2D grid line endpoints."
        },
        "show_bubble_at_start": {
          "description": "Shows the grid bubble at the starting coordinate.",
          "type": "boolean"
        },
        "end_offset": {
          "description": "The coordinate offset distance (feet) to extend or retract the end point of the grid line in a target view.",
          "type": "number"
        },
        "arc_point_x": {
          "description": "X coordinate of a third point along the arc in Revit's internal coordinate system (feet).",
          "type": "number"
        },
        "arc_point_y": {
          "description": "Y coordinate of a third point along the arc in Revit's internal coordinate system (feet).",
          "type": "number"
        },
        "start_z": {
          "description": "Starting Z coordinate in feet, in Revit's internal coordinate system.",
          "type": "number"
        },
        "end_z": {
          "description": "Ending Z coordinate in feet, in Revit's internal coordinate system.",
          "type": "number"
        },
        "grid_id": {
          "description": "The UniqueId of the Grid to modify.",
          "type": "string"
        },
        "start_offset": {
          "description": "The coordinate offset distance (feet) to extend or retract the start point of the grid line in a target view.",
          "type": "number"
        },
        "arc_point_z": {
          "description": "Z coordinate of a third point along the arc in Revit's internal coordinate system (feet).",
          "type": "number"
        },
        "start_angle": {
          "description": "Starting angle of the arc (radians).",
          "type": "number"
        },
        "center_x": {
          "description": "Center point X coordinate for radial arcs in Revit's internal coordinate system (feet).",
          "type": "number"
        },
        "target_view_id": {
          "description": "The UniqueId of the view where view-specific (2D) adjustments are applied.",
          "type": "string"
        },
        "show_bubble_at_end": {
          "description": "Shows the grid bubble at the ending coordinate.",
          "type": "boolean"
        },
        "name": {
          "description": "New display name of the grid. Must be unique.",
          "type": "string"
        },
        "start_x": {
          "description": "Starting X coordinate in feet, in Revit's internal coordinate system.",
          "type": "number"
        },
        "datum_extent_type": {
          "description": "Options are '2D' (view-specific) or '3D' (model-wide) when making graphic adjustments.",
          "type": "string"
        },
        "end_x": {
          "description": "Ending X coordinate in feet, in Revit's internal coordinate system.",
          "type": "number"
        },
        "scope_box_id": {
          "description": "The UniqueId of an existing Scope Box to control the grid's 3D boundary.",
          "type": "string"
        },
        "grid_type_id": {
          "description": "The UniqueId of the Grid Type family symbol to control line patterns and bubble styles.",
          "type": "string"
        },
        "end_angle": {
          "description": "Ending angle of the arc (radians).",
          "type": "number"
        },
        "end_y": {
          "description": "Ending Y coordinate in feet, in Revit's internal coordinate system.",
          "type": "number"
        },
        "maximize_3d_extents": {
          "description": "Forces the grid's 3D plane to automatically expand to encompass all model geometry.",
          "type": "boolean"
        },
        "radius": {
          "description": "Radius distance of the curved arc (feet).",
          "type": "number"
        },
        "center_y": {
          "description": "Center point Y coordinate for radial arcs in Revit's internal coordinate system (feet).",
          "type": "number"
        }
      }
    },
    "name": "modify_grid",
    "description": "Modifies an existing reference gridline in the active Revit project. Supports modifying name, curve geometry (linear or arc), scope box, extent type, bubble visibility, offsets, and propagation."
  },
  {
    "agent_instructions": "Before calling: Call fetch_grids to get the valid UniqueId for the grid_id parameter. Confirm with the user before deleting grids, as this action cannot be undone.",
    "parameters": {
      "required": [
        "grid_id"
      ],
      "type": "object",
      "properties": {
        "grid_id": {
          "description": "The UniqueId of the Grid to delete (from fetch_grids).",
          "type": "string"
        }
      }
    },
    "name": "delete_grid",
    "description": "Deletes an existing reference gridline from the active Revit project by its UniqueId."
  },
  {
    "agent_instructions": "Before calling: (1) Call fetch_levels to verify the name is unique and check current elevations. Elevation is in feet relative to the Project Base Point.\n(2) NEVER assume the name or elevation \u2014 if the user has not specified them explicitly, ASK the user for the exact level name and elevation before calling this tool.",
    "parameters": {
      "required": [
        "name",
        "elevation"
      ],
      "type": "object",
      "properties": {
        "start_offset": {
          "description": "The coordinate offset distance (feet) to extend or retract the start point of the level line in a target view.",
          "type": "number"
        },
        "level_type_id": {
          "description": "The UniqueId of the Level Type family symbol (determines line styles and head graphics).",
          "type": "string"
        },
        "scope_box_id": {
          "description": "The UniqueId of an existing Scope Box to control the level's 3D boundary.",
          "type": "string"
        },
        "view_template_id": {
          "description": "The UniqueId of an existing View Template to apply to the newly generated views.",
          "type": "string"
        },
        "target_view_id": {
          "description": "The UniqueId of the elevation/section view where view-specific (2D) adjustments are applied.",
          "type": "string"
        },
        "create_ceiling_plan": {
          "description": "Automatically creates an associated Reflected Ceiling Plan (RCP) view (defaults to False).",
          "type": "boolean"
        },
        "show_bubble_at_end": {
          "description": "Shows the level bubble head at the ending end of the line (defaults to True).",
          "type": "boolean"
        },
        "name": {
          "description": "Unique display name of the level (e.g. '01 - FIRST FLOOR').",
          "type": "string"
        },
        "datum_extent_type": {
          "description": "Options are '2D' (view-specific) or '3D' (model-wide) when making graphic adjustments.",
          "type": "string"
        },
        "propagate_to_views": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "A list of target Elevation/Section view UniqueIds that should copy the same 2D level line endpoints."
        },
        "show_bubble_at_start": {
          "description": "Shows the level bubble head at the starting end of the line (defaults to False).",
          "type": "boolean"
        },
        "end_offset": {
          "description": "The coordinate offset distance (feet) to extend or retract the end point of the level line in a target view.",
          "type": "number"
        },
        "is_structural": {
          "description": "Designates the level as a structural datum for analytical models.",
          "type": "boolean"
        },
        "create_structural_plan": {
          "description": "Automatically creates an associated Structural Plan view (defaults to False).",
          "type": "boolean"
        },
        "maximize_3d_extents": {
          "description": "Forces the level's 3D plane to automatically expand to encompass all model geometry.",
          "type": "boolean"
        },
        "elevation": {
          "description": "The height of the level in feet, relative to the Project Base Point.",
          "type": "number"
        },
        "create_floor_plan": {
          "description": "Automatically creates an associated Floor Plan view (defaults to True).",
          "type": "boolean"
        }
      }
    },
    "name": "create_level",
    "description": "Creates a new horizontal level in the active Revit project. Optionally creates plan views (Floor Plan, Ceiling Plan, Structural Plan) and applies a view template."
  },
  {
    "agent_instructions": "Before calling: (1) Call fetch_levels to get the valid UniqueId for the level_id parameter.",
    "parameters": {
      "required": [
        "level_id"
      ],
      "type": "object",
      "properties": {
        "name": {
          "description": "New display name of the level. Must be unique.",
          "type": "string"
        },
        "target_view_id": {
          "description": "The UniqueId of the elevation/section view where view-specific (2D) adjustments are applied.",
          "type": "string"
        },
        "create_structural_plan": {
          "description": "Creates associated Structural Plan if it does not already exist.",
          "type": "boolean"
        },
        "end_offset": {
          "description": "The coordinate offset distance (feet) to extend or retract the end point of the level line in a target view.",
          "type": "number"
        },
        "scope_box_id": {
          "description": "The UniqueId of an existing Scope Box to control the level's 3D boundary.",
          "type": "string"
        },
        "show_bubble_at_start": {
          "description": "Shows the level bubble head at the starting end.",
          "type": "boolean"
        },
        "create_floor_plan": {
          "description": "Creates associated Floor Plan if it does not already exist.",
          "type": "boolean"
        },
        "start_offset": {
          "description": "The coordinate offset distance (feet) to extend or retract the start point of the level line in a target view.",
          "type": "number"
        },
        "show_bubble_at_end": {
          "description": "Shows the level bubble head at the ending end.",
          "type": "boolean"
        },
        "create_ceiling_plan": {
          "description": "Creates associated Reflected Ceiling Plan (RCP) if it does not already exist.",
          "type": "boolean"
        },
        "level_type_id": {
          "description": "The UniqueId of the Level Type family symbol.",
          "type": "string"
        },
        "level_id": {
          "description": "The UniqueId of the Level to modify.",
          "type": "string"
        },
        "view_template_id": {
          "description": "The UniqueId of a View Template to apply to newly generated views.",
          "type": "string"
        },
        "datum_extent_type": {
          "description": "Options are '2D' (view-specific) or '3D' (model-wide) when making graphic adjustments.",
          "type": "string"
        },
        "propagate_to_views": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "A list of target Elevation/Section view UniqueIds that should copy the same 2D level line endpoints."
        },
        "maximize_3d_extents": {
          "description": "Forces the level's 3D plane to automatically expand to encompass all model geometry.",
          "type": "boolean"
        },
        "elevation": {
          "description": "New height of the level in feet, relative to the Project Base Point.",
          "type": "number"
        },
        "is_structural": {
          "description": "Designates the level as a structural datum for analytical models.",
          "type": "boolean"
        }
      }
    },
    "name": "modify_level",
    "description": "Modifies an existing horizontal level in the active Revit project. Optionally creates plan views (Floor Plan, RCP, Structural Plan) and applies a view template."
  },
  {
    "agent_instructions": "Before calling: Call fetch_levels to get the valid UniqueId for the level_id parameter. IMPORTANT: Always confirm with the user before deleting a level, as this will also delete associated views and hosted elements. This action cannot be undone.",
    "parameters": {
      "required": [
        "level_id"
      ],
      "type": "object",
      "properties": {
        "level_id": {
          "description": "The UniqueId of the Level to delete (from fetch_levels).",
          "type": "string"
        }
      }
    },
    "name": "delete_level",
    "description": "Deletes an existing level from the active Revit project by its UniqueId. WARNING: Deleting a level also deletes all associated views (floor plans, ceiling plans, etc.) and any elements hosted on that level."
  },
  {
    "agent_instructions": "Before calling: (1) Call fetch_sheets to verify the sheet_number is not already used. Follow the project's existing sheet numbering convention as observed from the fetch_sheets results.\n(2) NEVER assume the sheet number or name \u2014 if the user has not specified them explicitly, ASK the user for the exact sheet_number and sheet_name before calling this tool.",
    "parameters": {
      "required": [
        "sheet_number",
        "sheet_name"
      ],
      "type": "object",
      "properties": {
        "sheet_number": {
          "description": "Unique sheet identifier code (e.g. 'A101', 'A102').",
          "type": "string"
        },
        "sheet_name": {
          "description": "Descriptive title of the sheet (e.g. 'FIRST FLOOR PLAN').",
          "type": "string"
        }
      }
    },
    "name": "create_sheet",
    "description": "Creates a new drawing sheet in the active Revit project using the first available Title Block family."
  }
]