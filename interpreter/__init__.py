"""Deterministic language interpretation package.

The interpreter converts controlled user language into payloads only. It never
executes Revit operations and never bypasses runtime validation or approval.
"""
